<%= form_for(nsm_asset_type) do |f| %>
  <% if nsm_asset_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nsm_asset_type.errors.count, "error") %> prohibited this nsm_asset_type from being saved:</h2>

      <ul>
      <% nsm_asset_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
